import { LeaveDaysPipe } from './leave-days.pipe';

describe('LeaveDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new LeaveDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
