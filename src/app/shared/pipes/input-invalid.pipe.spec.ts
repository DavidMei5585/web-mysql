import { InputInvalidPipe } from './input-invalid.pipe';

describe('InputInvalidPipe', () => {
  it('create an instance', () => {
    const pipe = new InputInvalidPipe();
    expect(pipe).toBeTruthy();
  });
});
